import mongoose from 'mongoose';

const districtDomicileWnaSchema = new mongoose.Schema(
  {
    
  },
);

const DistrictDomicileWna = mongoose.model('DistrictDomicileWna', districtDomicileWnaSchema);
export default DistrictDomicileWna;
